# selections.yaml — slide-id -> {model, variation, instance}
# Set after reviewing draft.pptx, then run `forge build` to render final.
# Example:
#   hero:
#     model: gpt-image-1
#     variation: 2
#     instance: 1
{}
