# Microsoft Foundry endpoint (no trailing slash, no path).
# Authentication is always Microsoft Entra ID via DefaultAzureCredential —
# run `az login` locally or rely on Managed Identity in Azure.
AZURE_FOUNDRY_ENDPOINT=https://tomaskubica-foundry-resource.services.ai.azure.com
